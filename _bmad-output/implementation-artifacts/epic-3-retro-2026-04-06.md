# Epic 3 Retrospective: Setup, Validation & Queue — "Bereit für die Nacht"

**Date:** 2026-04-06
**Facilitator:** Bob (Scrum Master)
**Participants:** Julian (Project Lead), Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer)

## Epic Summary

| Metric | Epic 1 | Epic 2 | Epic 3 | Trend |
|---|---|---|---|---|
| Stories | 3/3 | 4/4 | 4/4 | Stable |
| Tests | 32 | 83 (+51) | 115 (+32) | +38% |
| Source Files | 27 | 36 (+9) | 38 (+2) | Efficient (stubs replaced) |
| Code Reviews | 2 (1 patch) | 3 (7 patches) | 1 (3 patches) | Fewer, more targeted |
| Blockers | 0 | 0 | 0 | Stable |
| Deferred Items | 6 | 14 (+8) | 24 (+10) | Growing |

### Stories Delivered

1. **3.1 Config Loader & Project Root Discovery** — ConfigLoader with upward search, default merging, mandatory field validation
2. **3.2 hpnc init Command** — Creates _hpnc/ directory, connectivity checks (Claude Code, Codex), BMAD detection, safe re-init
3. **3.3 Queue Manager & hpnc queue add** — Frontmatter parsing from markdown, queue add/list, duplicate detection
4. **3.4 Validation Engine & hpnc validate** — Pre-flight checks (night_ready, mandatory fields, blocking_questions, worktree, secrets hook), reports all failures

## Epic 2 Action Item Follow-Through

| Action Item | Status | Evidence |
|---|---|---|
| Finalize story files properly | Partial | 3.1+3.2 done, 3.3+3.4 skipped in fast flow |
| Run epic-wide review | Completed | Found 3 patches (YAML injection, CR/LF, protected_paths) |
| Error handling pattern template | N/A | No new TaskRunner-style code in Epic 3 |
| Track deferred debt centrally | In retro docs | No separate file yet |

## What Went Well

- **Higher velocity than Epic 2** — 4 stories with fewer new files because stubs from Epic 1 were replaced
- **Epic-wide review continues to pay off** — YAML injection fix in init_cmd was a real security issue
- **CR/LF fix prevents Windows-specific bugs** — would have caused silent frontmatter mismatches
- **Test suite grows steadily** — 32 → 83 → 115, zero regressions throughout
- **All CLI commands now functional** — init, validate, queue add work end-to-end

## What Could Be Improved

- **Deferred items keep growing** (6 → 14 → 24) — need to address before they become unmanageable
- **Story files not always finalized** — still skipping checkboxes on some stories in fast flow
- **No E2E CLI tests** — we test core logic but not the Typer commands themselves

## Test Coverage Assessment

**115 tests at 78% coverage is appropriate for Phase 1.**

| Category | Coverage | Assessment |
|---|---|---|
| Core logic (state machine, gates, schemas) | 94-100% | Excellent |
| Infrastructure (workspace, config, git) | 79-96% | Good, gaps are error paths |
| TaskRunner | 72% | Error handlers untested |
| ProcessLock | 78% | POSIX branches unreachable on Windows |
| CLI commands | 0-89% | Logic tested, Typer wiring not |

**Coverage gaps are in the right places** — error recovery paths and CLI wiring, not in business logic or quality gates (NFR7).

## Deferred Technical Debt (cumulative — 24 items)

### From Epic 1 (6)
- ProcessLock TOCTOU race, TRANSITIONS mutable dict, HpncError pickle, Phase 2 states misleading error, CI concurrency group, No subprocess timeout

### From Epic 2 (+8)
- Pipe deadlock in real executors, Exit codes >= 2 mapped to TIMEOUT, Pre-existing branch/worktree recovery, Branch cleanup on failure, SecretsGate substring false positive, GateResult/GateResults mutability, No integration tests for error paths, SecretsGate UnicodeDecodeError

### From Epic 3 (+10)
- Validator boolean bypass in mandatory check, parse_frontmatter silently swallows YAML errors, Windows .cmd/.bat CLI resolution, ConfigLoader symlink infinite loop, QueueManager TOCTOU concurrent add, Queue corruption returns empty, Validator TOCTOU vs night run, Init permission errors unhandled, Duplicate path case-sensitivity Windows, No subprocess timeout (reinforced)

## Action Items for Epic 4

1. **Address deferred items relevant to Epic 4** — pre-existing branch/worktree recovery is directly needed for Dispatcher
2. **Add E2E CLI test** — at minimum `hpnc --help` and `hpnc init` in a temp dir
3. **Create deferred-work.md** — centralize all 24 deferred items in one trackable file
4. **Story file hygiene** — automate or at minimum checklist before marking done

## Epic 4 Readiness Assessment

**Status: READY TO START**

- ConfigLoader finds project root from anywhere
- QueueManager reads night-queue.yaml
- Validator validates all queued stories
- TaskRunner processes individual tasks through complete lifecycle
- All infrastructure (Workspace, GitWrapper, ProcessLock, Logging) in place
- MockAgentExecutor enables token-free testing
- Epic 4 builds Dispatcher (orchestrates TaskRunner for queue), Scheduling (--at, --delay), and Report Generator (hpnc status)
